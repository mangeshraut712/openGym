# Copy to `.env` before `docker compose up`. See docs/SELF_HOSTING.md.
RP_ID=localhost
ORIGIN=http://localhost:8080
WEB_PORT=8080
RP_NAME=openGym
ADMIN_UIDS=
INVITE_ONLY=0
# SESSION_DAYS=90
# VAPID_SUBJECT=mailto:admin@localhost
